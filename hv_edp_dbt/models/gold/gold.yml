version: 2

models:
# ================
# KURTOSYS GOLD MODELS
# ================
  - name: gold_fund_metrics
    description: "Gold Fund Metrics"
    columns:
      - name: as_of_date
        tests: [not_null]
  - name: gold_portfolio_metrics
    description: "Gold Portfolio Metrics"
    columns:
      - name: as_of_date
        tests: [not_null]
  - name: gold_company_metrics
    description: "Gold Company Metrics"
    columns:
      - name: as_of_date
        tests: [not_null]
  - name: gold_holding_metrics
    description: "Gold Holding Metrics"
    columns:
      - name: as_of_date
        tests: [not_null]
  - name: gold_investor_performance_metrics
    description: "Gold Investor Metrics"
    columns:
      - name: as_of_date
        tests: [not_null]

  - name: gold_investor_daily_transaction_metrics
    description: "Gold Investor Daily Transaction Metrics"
    columns:
      - name: as_of_date
        tests: [not_null]
