version: 2

models:
  - name: gold_funds
    description: Funds gold data.
    config:
      incremental_strategy: merge
      unique_key: type
    columns:
      - name: type
        description: Primary key
        data_tests:
          - unique
          - not_null