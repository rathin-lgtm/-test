version: 2

models:
  - name: gold_funds
    description: Funds gold data.
    config:
      incremental_strategy: merge
      unique_key: type
      meta:
        dagster:
          group: "DIM_FUND"
    columns:
      - name: type
        description: Primary key
        data_tests:
          - unique
          - not_null