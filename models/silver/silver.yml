version: 2

models:
  - name: silver_funds
    description: Funds silver data.
    config:
      meta:
        dagster:
          group: "DIM_FUNDS"
    columns:
      - name: fund_id
        description: Primary key
        data_tests:
          - not_null
          - unique

